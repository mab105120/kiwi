openapi: 3.1.0
info:
  title: app-api
  version: 0.1.0
  description: TODO - exam authoring/hosting, assignment authoring, feedback.
paths:
  /app-api/healthz:
    get:
      summary: Health check
      operationId: getHealthz
      responses:
        "200":
          description: Service is healthy
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: ok
